# 已掌握导出属性与场景覆盖值

学习者已在贪吃蛇项目中用 `@export_range` 将初始速度暴露到 Inspector，并根据实际保存结果确认：脚本提供属性默认值，而 `.tscn` 中保存的场景覆盖值在运行时优先。这为后续理解场景实例的独立配置和 Resource 序列化打下基础。

## Evidence

学习者将 `start_speed` 在场景中设为 `0.4`，并正确回答“场景覆盖值优先”。
