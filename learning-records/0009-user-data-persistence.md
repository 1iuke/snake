# 已掌握 user:// 玩家数据持久化

学习者已使用 ConfigFile 将最高分和游戏统计保存到用户数据目录，并理解开发时 `res://` 可能可写，但导出后的项目资源通常为只读；`user://` 才是 Godot 在不同平台上保证可写的持久化位置。

## Evidence

项目已实际生成包含最高分的 `user://scores.cfg` 并能重新加载，Godot 4.7.2 运行验证通过；学习者正确指出 `res://` 的只读发布语义。
